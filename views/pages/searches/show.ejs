<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>
</head>
<body>
    <header>
        <h1>Results Of Your Search</h1>
        <nav>
          <ul>
            <li><a href="searches/new">New Search</a></li>
          </ul>
        </nav>
      </header>
    <main>
        <ul>
           <% for (let i = 0; i < results.length; i++) {  %>
            <li>
                <p>Title: <%= results[i].title %></p>
                <p>Author: <%= results[i].author %></p>
                <p>ISBN Number: <%= results[i].isbn %></p>
                <img src=<%= results[i].thumbnail %> />
                <p> <%= results[i].description %></p> 
                <form action="/books" method="POST">
                <input type="hidden" value="<%=results[i].title%>" name="title">
                <input type="hidden" value="<%=results[i].author%>" name="author">
                <input type="hidden" value="<%=results[i].isbn%>" name="isbn">
                <input type="hidden" value="<%=results[i].thumbnail%>" name="thumbnail">
                <input type="hidden" value="<%=results[i].description%>" name="description">
                <button type="submit">Save Book</button>
                </form>
            </li>

            <% }  %>
        </ul>
    </main>
</body>
</html>